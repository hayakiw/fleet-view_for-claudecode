---
name: code-review-checklist
description: 構造化されたコードレビューの観点とチェック項目。直近のコミットや差分、特定のファイル・機能をレビューする際に使う。
---

# コードレビュー手順

以下の観点を順に確認し、見つけた指摘は重大度順にまとめて報告する。

## 1. 正確性
- ロジックが意図通りか。エッジケース（空・null・境界値・並行処理）は考慮されているか
- 既存の挙動を壊していないか

## 2. セキュリティ
- ユーザー入力がそのままSQL/シェルコマンド/HTMLに渡っていないか（インジェクション、XSS）
- 認証・認可のチェック漏れはないか
- 秘密情報（APIキー、パスワード、トークン）がハードコードされていないか

## 3. エラーハンドリング
- 失敗しうる処理（外部呼び出し、パース、I/O）に適切な処理があるか
- エラーメッセージが内部情報を過剰に露出していないか

## 4. テスト
- 変更箇所に対応するテストがあるか、既存テストは通るか

## 5. 一貫性・簡潔さ
- 既存のコード規約・命名に沿っているか
- 不要に複雑になっていないか

## 報告の仕方
- 重大度（Critical / Warning / Nit）ごとに分類する
- 該当ファイル・行を明示する
- 指摘のみ行い、指示がない限りコード自体は修正しない
