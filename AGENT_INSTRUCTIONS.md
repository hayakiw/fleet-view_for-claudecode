# 自動開発エージェント向け指示書

このファイルは、`TASKS.md` のバックログを消化する開発エージェント向けの作業ルールです。
起動は手動のみを想定しています（Windowsタスクスケジューラやクラウド上のRoutineは使わない
方針。ユーザーが `/loop` 等で都度呼び出します）。

## やること（1回の起動につき1件だけ）

1. `TASKS.md` を読み、上から最初の未完了項目（`- [ ]`）を1つだけ選ぶ。
2. このリポジトリ（`C:\Users\hayak\work\dojo\agent`）の中だけで実装する。
   既存のコード規約に従うこと:
   - バックエンド: `server/` 配下、Node.js + Express + ws。ビルド不要のESM。
   - フロントエンド: `web/` 配下、素のHTML/CSS/JS。ビルドツール・外部CDN禁止（自己完結）。
   - UI文言は日本語。既存のダークテーマ（シアン/バイオレット基調）に合わせる。
   - HTMLへの動的値挿入は必ず `escapeHtml()` を通す（XSS対策）。
   - コメントは最小限。WHYが非自明な場合のみ短く。
3. 実装後、必ず動作確認する:
   - 変更した `.js` ファイルは `node --check <file>` で構文確認。
   - `server/` 配下を変更した場合はサーバーを再起動して確認する:
     ```
     netstat -ano | grep ":4317" | grep LISTENING   # 現PIDを確認
     taskkill //F //PID <pid>
     cd C:\Users\hayak\work\dojo\agent && nohup node server/index.js > /tmp/fleetview.log 2>&1 &
     disown
     curl -s http://localhost:4317/api/sessions       # 生存確認
     ```
   - `web/` 配下のみの変更ならサーバー再起動は不要。`curl -s -o /dev/null -w "%{http_code}" http://localhost:4317/<file>` で配信確認すれば十分。
   - 新機能には、可能な範囲でAPI呼び出しやイベント投入によるスモークテストを行うこと
     （このプロジェクトのこれまでの開発と同じやり方 = `hooks/report-event.mjs` にJSONを
     パイプして疑似イベントを送り、`/api/sessions/...` で結果を確認する等）。
   - テストに使った一時的なセッションIDやイベントは、確認後に `data/state.json` /
     `data/events.jsonl` から取り除いてクリーンな状態に戻すこと。
4. `TASKS.md` の該当項目を `- [x]` に変更し、行末に完了日を追記する
   （commitハッシュは、コード変更をコミットした後に別コミットで追記する。手順は下記）。
5. Gitコミットする（**pushは絶対にしない** — リモートは設定されていない前提）:
   - まずコード変更をコミット（1機能=1コミット、コミットメッセージは "why" 重視で簡潔に）。
   - `git log -1 --format=%h HEAD` で短縮SHAを取得。
   - `TASKS.md` のチェックボックスを更新し、`(完了: YYYY-MM-DD, commit <short-sha>)` を追記して
     別コミットする（例: `docs: mark backlog item complete`）。

## やらないこと

- `TASKS.md` にない大規模な新機能を勝手に追加しない。今回のタスクをこなす過程で
  思いついた小さな改善候補があれば、`TASKS.md` の末尾に1行追記するのは構わないが、
  それ自体を実装まではしない。
- 未完了項目が1つもない場合は、何もせず終了する（新しいタスクを作り出さない）。
- `git push`、リモート追加、force操作は絶対に行わない。
- このリポジトリ以外のファイル・他プロジェクトには一切触れない。
- 既存の動いている機能を壊すような大規模リファクタはしない。差分は最小限に。

## 完了の判断に迷ったら

タスクの粒度が大きすぎる、要件が曖昧、などで実装を進められない場合は、無理に進めず
`TASKS.md` にその項目を残したまま、代わりに疑問点を短くコメントとして
`TASKS.md` の当該項目の下に追記して終了してよい（例: `> 要確認: 通知許可のUIをどこに置くか`）。
