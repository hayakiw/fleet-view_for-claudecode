# TASKS — FleetView 改善バックログ

自動開発エージェント向けのバックログです。上から順に、未完了(`- [ ]`)の項目を
**1件だけ**選んで実装・動作確認・コミットしてください。詳細なルールは
`AGENT_INSTRUCTIONS.md` を参照してください。

完了したら `- [x]` に変更し、末尾に `(完了: YYYY-MM-DD, commit <short-sha>)` を追記します。

---

- [ ] 設計を進める

- [x] [reviewer発見] CSRF/DNS再バインディング対策の不足 — a31b6dbでlocalhost限定バインドに修正されたが、`/api/tasks/run`（server/index.js、自動承認モードでエージェントを起動しファイル編集・コミットする）や `/api/tasks` の追加・削除・完了APIは認証・CSRFトークン・Origin検証が一切ない。ユーザーがサーバー稼働中に悪意あるWebページを開くと、単純なPOSTリクエスト（プリフライト不要）でこれらのエンドポイントを黙って呼び出せてしまう。Originヘッダ検証やCSRFトークンの導入を検討する。 (完了: 2026-07-23)
