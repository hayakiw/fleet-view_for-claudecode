# TASKS — FleetView 改善バックログ

自動開発エージェント向けのバックログです。上から順に、未完了(`- [ ]`)の項目を
**1件だけ**選んで実装・動作確認・コミットしてください。詳細なルールは
`AGENT_INSTRUCTIONS.md` を参照してください。

完了したら `- [x]` に変更し、末尾に `(完了: YYYY-MM-DD, commit <short-sha>)` を追記します。

---

- [ ] セッション削除・非表示API — GUIまたはAPI経由で、不要な古いセッション/テスト用セッションを 一覧から手動で消せるようにする（現状はdata/state.jsonを直接編集するしかない）。 `DELETE /api/sessions/:id` を追加し、ダッシュボードの各カードに削除ボタンを付ける。

- [ ] 「入力待ち」状態のデスクトップ通知 — セッションが waiting_input になった瞬間、 ブラウザのNotification APIでデスクトップ通知を出す（タブを見ていなくても気づけるように）。 初回アクセス時に通知許可をリクエストする導線も入れる。

- [ ] レポート期間のプリセット選択UI — 現在は「今すぐレポート生成」ボタンで24時間固定になっている。 1時間/24時間/7日間など期間を選べるドロップダウンをレポートタブに追加する。

- [ ] イベントログ(data/events.jsonl)のローテーション — 現状無制限に追記され続けるため、 一定サイズ/一定期間を超えたら古い行を切り捨てる、または月別ファイルに分割する仕組みを入れる。

- [ ] ダッシュボードにセッション検索/フィルタ — プロジェクト名やステータスでカードを絞り込める 検索ボックスをヘッダーに追加する。

- [ ] 「ツール実行中」カードに経過時間表示 — PreToolUseからの経過秒数をカード上にリアルタイム表示し、 同じツールが異常に長く動いている（ハング疑い）ケースに気づけるようにする。

- [ ] macOS/Linux向けhooksセットアップ手順の整備 — 現状 hooks/settings-snippet.json は Windowsパス前提。OS判定してコマンドを出し分けるセットアップスクリプト、または READMEにOS別の手順を追記する。

- [ ] レポートのプロジェクト内訳にツール使用の割合を可視化 — 現在はテキストの箇条書きのみ。 簡単なテキストベースの棒グラフ（例: `Bash ████████ 40%`）を追加して視覚的にする。
